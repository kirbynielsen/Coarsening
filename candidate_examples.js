/* Static, non-interactive examples of the Part 1 and Part 2 interfaces, rendered
   with the same chip styling as the live widgets so instructions always match
   the current look and the participant's treatment (number of attributes).

   Usage in a Qualtrics question (one or more per page):
     <div class="pm-example">{ ...spec JSON... }</div>
   and load this script in the question's addOnload. Specs:
     { "kind": "overview", "nAttributes": ${e://Field/numattributes} }
         -> data table (demo rows, 50% success), empty query builder, chip tray
     { "kind": "query", "nAttributes": 2, "conditions": [...], "target": [...],
       "showBuilder": true, "showResult": false, "pct": 50 }
         -> the builder filled in (showBuilder) and/or the saved-result line (showResult)
     { "kind": "choice", "nAttributes": 2, "numRounds": 42,
       "a": ["high","low"], "b": ["low","high"], "showTie": true }
         -> one frozen round of the choice task
   Terms in "conditions" / "target": {"attr": <displayed position>, "value": "high"|"low"}
   or {"outcome": "successful"|"unsuccessful"}, plus "conn": "and"|"or" on every term
   after the first. Nothing here logs, saves, or touches the data pipeline. */
(function(){
  var CSS = "\n:root {\n  --color-background-primary: #ffffff;\n  --color-background-secondary: #f7f6f2;\n  --color-background-tertiary: #efede5;\n  --color-background-info: #E6F1FB;\n  --color-text-primary: #1a1a1a;\n  --color-text-secondary: #5f5e5a;\n  --color-text-tertiary: #888780;\n  --color-text-info: #185FA5;\n  --color-text-warning: #854F0B;\n  --color-border-tertiary: rgba(0,0,0,0.15);\n  --color-border-secondary: rgba(0,0,0,0.3);\n  --color-border-info: #378ADD;\n  --border-radius-md: 8px;\n  --border-radius-lg: 12px;\n  --font-sans: -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif;\n  --font-mono: 'SF Mono', Menlo, Consolas, monospace;\n\n  --light1-fill: #EF9F27; --light1-stroke: #BA7517; --light1-bg: #FAEEDA; --light1-text: #854F0B; --light1-glow: #FAC775;\n  --light2-fill: #1D9E75; --light2-stroke: #0F6E56; --light2-bg: #E1F5EE; --light2-text: #085041; --light2-glow: #9FE1CB;\n  --light3-fill: #7F77DD; --light3-stroke: #534AB7; --light3-bg: #EEEDFE; --light3-text: #3C3489; --light3-glow: #CECBF6;\n  --light4-fill: #D9635B; --light4-stroke: #B0433C; --light4-bg: #FBE9E7; --light4-text: #7A2521; --light4-glow: #F0AFAA;\n  --light1-low: #ECD8BE; --light1-lowedge: #D9B37F; --light2-low: #BCD6D0; --light2-lowedge: #7BAFA2; --light3-low: #CFCCEB; --light3-lowedge: #A09BD7; --light4-low: #E9CAC8; --light4-lowedge: #D49894;\n  --sound-fill: #185FA5; --sound-bg: #E6F1FB; --sound-text: #042C53;\n}\nh2 { font-size: 14px; font-weight: 500; color: var(--color-text-secondary); margin: 0 0 8px; display: flex; justify-content: space-between; align-items: baseline; }\nh2 .meta { font-size: 11px; color: var(--color-text-tertiary); font-weight: 400; }\n\n.database-box { height: 200px; overflow-y: auto; border: 0.5px solid var(--color-border-tertiary); border-radius: var(--border-radius-md); background: var(--color-background-primary); margin-bottom: 1.5rem; }\n.database-box table { width: 100%; border-collapse: collapse; font-size: 12px; }\n.database-box thead { position: sticky; top: 0; background: var(--color-background-secondary); z-index: 1; }\n.database-box th { padding: 8px 6px; text-align: center; font-weight: 500; color: var(--color-text-secondary); font-size: 11px; text-transform: uppercase; letter-spacing: 0.04em; border-bottom: 0.5px solid var(--color-border-tertiary); }\n.database-box td { padding: 5px 6px; text-align: center; border-bottom: 0.5px solid var(--color-border-tertiary); }\n.database-box td.machine-id { font-family: var(--font-mono); color: var(--color-text-tertiary); font-size: 11px; }\n.bulb-cell { display: inline-block; width: 11px; height: 11px; border-radius: 50%; vertical-align: middle; }\n.bulb-cell.off { background: transparent; border: 1.5px solid var(--color-text-tertiary); }\n.sound-yes { color: #0F6E56; font-weight: 500; }\n.sound-no { color: var(--color-text-tertiary); }\n\n.instructions { font-size: 13px; color: var(--color-text-secondary); margin-bottom: 1rem; line-height: 1.6; }\n.instructions strong { color: var(--color-text-primary); font-weight: 500; }\n\n/* control panel */\n.controls { background: var(--color-background-primary); border: 0.5px solid var(--color-border-tertiary); border-radius: var(--border-radius-lg); padding: 16px 18px; margin-bottom: 1rem; }\n\n/* query builder */\n.qb-line { display: flex; flex-wrap: wrap; align-items: center; gap: 8px; padding: 10px 0; border-top: 0.5px solid var(--color-border-tertiary); }\n.qb-line:first-child { border-top: none; padding-top: 2px; }\n.qb-text { font-size: 15px; color: var(--color-text-primary); }\n.qb-blank { display: inline-flex; flex-wrap: wrap; align-items: center; gap: 6px; min-width: 90px; min-height: 26px; padding: 3px 6px; border-bottom: 1.5px dashed var(--color-border-secondary); }\n.qb-blank.empty::before { content: 'all candidates'; color: var(--color-text-tertiary); font-style: italic; font-size: 13px; }\n.hilo { font-size: 10px; color: var(--color-text-tertiary); text-transform: uppercase; letter-spacing: 0.03em; }\n.qb-blank.empty.target-empty::before { content: '\u2014 pick at least one \u2014'; color: var(--color-text-warning); }\n.qb-chip { display: inline-flex; align-items: center; gap: 5px; }\n\n/* AND-group brackets */\n.qb-blank { row-gap: 8px; }\n.qb-group { display: inline-flex; align-items: center; gap: 7px; padding: 6px 9px; background: var(--color-background-primary); border: 1px solid var(--color-border-tertiary); border-radius: var(--border-radius-md); }\n.qb-group.solo { border-style: none; background: transparent; padding: 2px 0; }\n.qb-or-sep { display: inline-flex; align-items: center; margin: 0 3px; }\n\n/* click-to-toggle AND / OR connector pill */\n.qb-conn { font-size: 10px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.05em; padding: 3px 10px; border-radius: 999px; cursor: pointer; border: 1px solid #3d3c38; background: #3d3c38; color: #fff; font-family: var(--font-sans); vertical-align: middle; transition: background 0.1s, color 0.1s, border-color 0.1s; }\n.qb-conn:hover { filter: brightness(1.2); }\n.qb-conn.or { background: var(--color-text-info); border-color: var(--color-text-info); color: #fff; }\n.qb-conn.ghost { opacity: 0.45; background: transparent; color: var(--color-text-secondary); border-style: dashed; border-color: var(--color-border-secondary); }\n.expr-conn { color: var(--color-text-primary); }\n\n.qb-trailer { display: inline-flex; align-items: center; gap: 7px; }\n.qb-ghost-chip { font-size: 11px; font-style: italic; color: var(--color-text-tertiary); border: 1px dashed var(--color-border-secondary); border-radius: 999px; padding: 3px 10px; opacity: 0.7; user-select: none; }\n.qb-add-wrap { position: relative; display: inline-block; }\n.qb-add-btn { font-size: 12px; font-weight: 500; padding: 5px 12px; background: var(--color-background-secondary); border: 0.5px solid var(--color-border-secondary); border-radius: 999px; cursor: pointer; color: var(--color-text-secondary); font-family: var(--font-sans); transition: background 0.12s, color 0.12s; }\n.qb-add-btn:hover:not(:disabled) { background: var(--color-text-info); color: #fff; border-color: var(--color-text-info); }\n.qb-add-btn.open { background: var(--color-text-info); color: #fff; border-color: var(--color-text-info); }\n.qb-add-btn:disabled { opacity: 0.35; cursor: not-allowed; }\n.qb-add-btn.mini { padding: 4px 11px; font-size: 15px; line-height: 1; font-weight: 600; }\n\n/* drop zones */\n.qb-zone-line { display: flex; flex-wrap: wrap; align-items: center; gap: 10px; padding: 7px 0; }\n.qb-zone-line.big { padding-top: 2px; }\n.qb-text-lg { font-size: 17px; color: var(--color-text-primary); font-weight: 500; }\n.qb-zone { flex: 1; min-width: 220px; display: flex; flex-wrap: wrap; align-items: center; gap: 8px; min-height: 46px; padding: 8px 10px; background: var(--color-background-secondary); border: 1.5px dashed var(--color-border-secondary); border-radius: var(--border-radius-md); transition: background 0.12s, border-color 0.12s; }\n.qb-zone.drop-hover { border-color: var(--color-border-info); border-style: solid; background: var(--color-background-info); }\n.qb-zone .qb-blank { border-bottom: none; padding: 0; min-width: 0; min-height: 0; }\n.qb-zone .qb-chip { cursor: grab; }\n.qb-zone .qb-chip:active { cursor: grabbing; }\n.qb-zone .qb-add-wrap { margin-left: auto; }\n\n/* chip tray / palette */\n.qb-palette-wrap { margin-top: 14px; border-top: 0.5px solid var(--color-border-tertiary); padding-top: 12px; }\n.qb-palette-label { font-size: 11px; text-transform: uppercase; letter-spacing: 0.04em; color: var(--color-text-tertiary); font-weight: 500; margin-bottom: 9px; }\n.qb-palette-label span { text-transform: none; letter-spacing: 0; font-weight: 400; }\n.qb-palette { display: flex; flex-wrap: wrap; gap: 10px; }\n.qb-pal-group { display: inline-flex; gap: 5px; padding: 5px; background: var(--color-background-secondary); border-radius: var(--border-radius-md); border: 0.5px solid var(--color-border-tertiary); }\n.pal-chip { cursor: grab; }\n.pal-chip:active { cursor: grabbing; }\n.qb-chip.dragging { opacity: 0.4; }\n.qb-palette.drop-remove { outline: 2px dashed var(--color-text-warning); outline-offset: 4px; border-radius: var(--border-radius-md); }\n\n.qb-menu { display: none; position: absolute; top: calc(100% + 5px); left: 0; z-index: 10; background: var(--color-background-primary); border: 0.5px solid var(--color-border-secondary); border-radius: var(--border-radius-lg); box-shadow: 0 8px 24px rgba(0,0,0,0.15); padding: 7px; width: 232px; }\n.qb-menu.open { display: block; }\n.qb-search { width: 100%; box-sizing: border-box; font-size: 13px; font-family: var(--font-sans); padding: 6px 9px; margin-bottom: 6px; border: 0.5px solid var(--color-border-tertiary); border-radius: 7px; outline: none; }\n.qb-search:focus { border-color: var(--color-border-info); box-shadow: 0 0 0 2px var(--color-background-info); }\n.qb-opt-list { max-height: 244px; overflow-y: auto; display: flex; flex-direction: column; gap: 1px; }\n.qb-opt { display: flex; align-items: center; justify-content: space-between; gap: 8px; padding: 4px 5px; border-radius: 7px; }\n.qb-opt:hover { background: var(--color-background-secondary); }\n.qb-opt-actions { display: inline-flex; gap: 4px; flex-shrink: 0; }\n.qb-state-btn { font-size: 11px; font-weight: 600; padding: 3px 11px; border-radius: 999px; cursor: pointer; border: 0.5px solid var(--color-border-secondary); background: var(--color-background-primary); color: var(--color-text-secondary); font-family: var(--font-sans); transition: background 0.1s, color 0.1s, border-color 0.1s; }\n.qb-state-btn.on:hover { background: var(--light2-bg); border-color: var(--light2-fill); color: var(--light2-text); }\n.qb-state-btn.off:hover { background: var(--color-background-tertiary); border-color: var(--color-border-secondary); color: var(--color-text-primary); }\n.qb-menu .menu-empty { font-size: 12px; color: var(--color-text-tertiary); font-style: italic; padding: 10px 8px; text-align: center; }\n.controls-header { font-size: 11px; text-transform: uppercase; letter-spacing: 0.04em; color: var(--color-text-tertiary); margin-bottom: 12px; display: grid; grid-template-columns: 120px 1fr; gap: 12px; }\n.controls-header .var-col { font-weight: 500; }\n.controls-header .action-col { font-weight: 500; }\n\n.var-row { display: grid; grid-template-columns: 120px 1fr; gap: 12px; align-items: center; padding: 8px 0; border-top: 0.5px solid var(--color-border-tertiary); }\n.var-row:first-of-type { border-top: none; padding-top: 4px; }\n\n.var-name { display: inline-flex; align-items: center; gap: 8px; font-size: 13px; color: var(--color-text-primary); }\n.var-name .var-bulb { width: 14px; height: 14px; border-radius: 50%; }\n.var-name .var-bulb.l1 { background: var(--light1-fill); border: 1.5px solid var(--light1-stroke); }\n.var-name .var-bulb.l2 { background: var(--light2-fill); border: 1.5px solid var(--light2-stroke); }\n.var-name .var-bulb.l3 { background: var(--light3-fill); border: 1.5px solid var(--light3-stroke); }\n.var-name .var-bulb.sound { background: var(--sound-fill); border: 1.5px solid var(--sound-text); }\n\n/* segmented action selector */\n.action-selector { display: flex; flex-wrap: wrap; gap: 2px; background: var(--color-background-secondary); border-radius: var(--border-radius-md); padding: 3px; border: 0.5px solid var(--color-border-tertiary); width: 100%; box-sizing: border-box; }\n.action-btn { flex: 1 1 auto; font-size: 12px; padding: 6px 10px; background: transparent; border: none; border-radius: 5px; cursor: pointer; color: var(--color-text-secondary); font-family: var(--font-sans); text-align: center; white-space: nowrap; transition: background 0.12s, color 0.12s; }\n.action-btn:hover { color: var(--color-text-primary); }\n.action-btn.active { background: var(--color-background-primary); color: var(--color-text-primary); font-weight: 500; box-shadow: 0 1px 2px rgba(0,0,0,0.06); }\n.action-btn .sub { display: inline-flex; align-items: center; gap: 4px; }\n.action-btn .mini-dot { width: 8px; height: 8px; border-radius: 50%; display: inline-block; }\n.action-btn .mini-dot.on.l1 { background: var(--light1-fill); border: 1.5px solid var(--light1-stroke); }\n.action-btn .mini-dot.on.l2 { background: var(--light2-fill); border: 1.5px solid var(--light2-stroke); }\n.action-btn .mini-dot.on.l3 { background: var(--light3-fill); border: 1.5px solid var(--light3-stroke); }\n.action-btn .mini-dot.on.sound { background: var(--sound-fill); border: 1.5px solid var(--sound-text); }\n.action-btn .mini-dot.off { background: transparent; border: 1.5px solid var(--color-text-tertiary); }\n\n/* result section */\n.result-section { background: var(--color-background-primary); border: 0.5px solid var(--color-border-tertiary); border-radius: var(--border-radius-lg); padding: 16px 18px; margin-bottom: 1rem; }\n.result-header { display: flex; justify-content: space-between; align-items: baseline; margin-bottom: 12px; }\n.result-title { font-size: 11px; text-transform: uppercase; letter-spacing: 0.04em; color: var(--color-text-tertiary); font-weight: 500; }\n.result-meta { font-size: 12px; color: var(--color-text-secondary); font-family: var(--font-mono); }\n.result-meta .dim { color: var(--color-text-tertiary); }\n\n.spec-line { font-size: 13px; color: var(--color-text-secondary); line-height: 1.9; margin-bottom: 12px; padding: 10px 12px; background: var(--color-background-secondary); border-radius: var(--border-radius-md); }\n.spec-line .spec-label { font-size: 11px; text-transform: uppercase; letter-spacing: 0.04em; color: var(--color-text-tertiary); margin-right: 5px; font-weight: 500; }\n.spec-line .spec-empty { color: var(--color-text-tertiary); font-style: italic; }\n.spec-line .spec-row { display: block; }\n\n.mini-chip { display: inline-flex; align-items: center; gap: 5px; padding: 2px 8px; border: 1.5px solid var(--color-border-secondary); border-radius: 999px; font-size: 12px; vertical-align: middle; margin: 0 1px; background: var(--color-background-primary); }\n.mini-chip.state-off { background: var(--color-background-tertiary); color: var(--color-text-secondary); }\n.mini-chip.kind-sound.state-on { background: var(--sound-bg); border-color: var(--sound-fill); color: var(--sound-text); }\n.mini-chip.kind-var.sound { background: var(--sound-bg); border-color: var(--sound-fill); color: var(--sound-text); }\n.mini-bulb { width: 9px; height: 9px; border-radius: 50%; display: inline-block; flex-shrink: 0; }\n.mini-bulb.off { background: transparent; border: 1.5px solid currentColor; }\n.bulb-cell.l1.on { background: var(--light1-stroke); border: 1.5px solid var(--light1-text); }\n.bulb-cell.l1.off { background: var(--light1-low); border: 1.5px solid var(--light1-lowedge); }\n.mini-bulb.l1.on { background: var(--light1-stroke); border: 1.5px solid var(--light1-text); }\n.mini-bulb.l1.off { background: var(--light1-low); border: 1.5px solid var(--light1-lowedge); }\n.mini-chip.l1.state-on { background: #fff; border: 1px solid var(--color-border-tertiary); border-left: 4px solid var(--light1-stroke); border-radius: 6px; color: var(--color-text-primary); }\n.mini-chip.l1.state-off { background: #fff; border: 1px solid var(--color-border-tertiary); border-left: 4px solid var(--light1-lowedge); border-radius: 6px; color: var(--color-text-primary); }\n.mini-chip.kind-var.l1 { background: var(--light1-bg); border-color: var(--light1-fill); color: var(--light1-text); }\n.bulb-cell.l2.on { background: var(--light2-stroke); border: 1.5px solid var(--light2-text); }\n.bulb-cell.l2.off { background: var(--light2-low); border: 1.5px solid var(--light2-lowedge); }\n.mini-bulb.l2.on { background: var(--light2-stroke); border: 1.5px solid var(--light2-text); }\n.mini-bulb.l2.off { background: var(--light2-low); border: 1.5px solid var(--light2-lowedge); }\n.mini-chip.l2.state-on { background: #fff; border: 1px solid var(--color-border-tertiary); border-left: 4px solid var(--light2-stroke); border-radius: 6px; color: var(--color-text-primary); }\n.mini-chip.l2.state-off { background: #fff; border: 1px solid var(--color-border-tertiary); border-left: 4px solid var(--light2-lowedge); border-radius: 6px; color: var(--color-text-primary); }\n.mini-chip.kind-var.l2 { background: var(--light2-bg); border-color: var(--light2-fill); color: var(--light2-text); }\n.bulb-cell.l3.on { background: var(--light3-stroke); border: 1.5px solid var(--light3-text); }\n.bulb-cell.l3.off { background: var(--light3-low); border: 1.5px solid var(--light3-lowedge); }\n.mini-bulb.l3.on { background: var(--light3-stroke); border: 1.5px solid var(--light3-text); }\n.mini-bulb.l3.off { background: var(--light3-low); border: 1.5px solid var(--light3-lowedge); }\n.mini-chip.l3.state-on { background: #fff; border: 1px solid var(--color-border-tertiary); border-left: 4px solid var(--light3-stroke); border-radius: 6px; color: var(--color-text-primary); }\n.mini-chip.l3.state-off { background: #fff; border: 1px solid var(--color-border-tertiary); border-left: 4px solid var(--light3-lowedge); border-radius: 6px; color: var(--color-text-primary); }\n.mini-chip.kind-var.l3 { background: var(--light3-bg); border-color: var(--light3-fill); color: var(--light3-text); }\n.bulb-cell.l4.on { background: var(--light4-stroke); border: 1.5px solid var(--light4-text); }\n.bulb-cell.l4.off { background: var(--light4-low); border: 1.5px solid var(--light4-lowedge); }\n.mini-bulb.l4.on { background: var(--light4-stroke); border: 1.5px solid var(--light4-text); }\n.mini-bulb.l4.off { background: var(--light4-low); border: 1.5px solid var(--light4-lowedge); }\n.mini-chip.l4.state-on { background: #fff; border: 1px solid var(--color-border-tertiary); border-left: 4px solid var(--light4-stroke); border-radius: 6px; color: var(--color-text-primary); }\n.mini-chip.l4.state-off { background: #fff; border: 1px solid var(--color-border-tertiary); border-left: 4px solid var(--light4-lowedge); border-radius: 6px; color: var(--color-text-primary); }\n.mini-chip.kind-var.l4 { background: var(--light4-bg); border-color: var(--light4-fill); color: var(--light4-text); }\n.mini-chip .hilo { font-size: 10px; color: var(--color-text-tertiary); text-transform: uppercase; letter-spacing: 0.03em; }\n\n/* result table */\n.result-table { width: 100%; border-collapse: collapse; font-size: 12px; background: var(--color-background-primary); border-radius: var(--border-radius-md); overflow: hidden; border: 0.5px solid var(--color-border-tertiary); }\n.result-table th, .result-table td { padding: 8px 12px; text-align: left; border-bottom: 0.5px solid var(--color-border-tertiary); }\n.result-table th { background: var(--color-background-secondary); font-weight: 500; color: var(--color-text-secondary); font-size: 11px; text-transform: uppercase; letter-spacing: 0.04em; }\n.result-table tr:last-child td { border-bottom: none; }\n.result-table td.numeric { font-family: var(--font-mono); text-align: right; }\n.result-table td.cell-label { font-size: 12px; }\n.result-table td .pct-large { font-size: 14px; font-weight: 500; }\n.result-table td .pct-sub { color: var(--color-text-tertiary); font-size: 10px; margin-left: 4px; }\n.result-empty { font-size: 13px; color: var(--color-text-tertiary); font-style: italic; padding: 1.5rem; text-align: center; background: var(--color-background-secondary); border-radius: var(--border-radius-md); }\n.result-warning { font-size: 13px; color: var(--color-text-warning); font-style: italic; padding: 1.5rem; text-align: center; background: var(--color-background-secondary); border-radius: var(--border-radius-md); }\n\n/* actions */\n.actions-row { display: flex; gap: 10px; align-items: center; margin-bottom: 1.5rem; flex-wrap: wrap; }\n.submit-btn { font-size: 14px; font-weight: 500; padding: 9px 18px; background: var(--color-text-info); color: white; border: none; border-radius: var(--border-radius-md); cursor: pointer; }\n.submit-btn:hover:not(:disabled) { opacity: 0.88; }\n.submit-btn:disabled { opacity: 0.35; cursor: not-allowed; }\n.ctrl-btn { font-size: 12px; padding: 7px 12px; background: transparent; border: 0.5px solid var(--color-border-secondary); border-radius: var(--border-radius-md); cursor: pointer; color: var(--color-text-secondary); }\n.ctrl-btn:hover { background: var(--color-background-secondary); }\n.summary-line { font-size: 12px; color: var(--color-text-tertiary); margin-left: auto; font-family: var(--font-mono); }\n\n/* history */\n.history-section { margin-top: 1.5rem; }\n.history-header { font-size: 13px; font-weight: 500; color: var(--color-text-secondary); margin-bottom: 10px; display: flex; justify-content: space-between; }\n.history-empty { font-size: 13px; color: var(--color-text-tertiary); font-style: italic; padding: 1rem; text-align: center; background: var(--color-background-secondary); border-radius: var(--border-radius-md); }\n.history-item { display: flex; align-items: center; gap: 8px; padding: 9px 12px; background: var(--color-background-primary); border: 0.5px solid var(--color-border-tertiary); border-radius: var(--border-radius-md); margin-bottom: 7px; }\n.history-item.latest { border-color: var(--color-border-info); background: var(--color-background-info); }\n.history-item[draggable=\"true\"] { cursor: grab; }\n.history-item.dragging { opacity: 0.4; cursor: grabbing; }\n.history-item.drag-over { border-color: var(--color-border-info); box-shadow: 0 -2px 0 0 var(--color-border-info); }\n.drag-handle { cursor: grab; color: var(--color-text-tertiary); user-select: none; letter-spacing: -2px; flex-shrink: 0; }\n.history-num { font-size: 11px; color: var(--color-text-tertiary); font-family: var(--font-mono); flex-shrink: 0; }\n.history-del { margin-left: auto; flex-shrink: 0; width: 22px; height: 22px; border-radius: 50%; border: 0.5px solid var(--color-border-secondary); background: var(--color-background-primary); color: var(--color-text-tertiary); font-size: 14px; line-height: 1; cursor: pointer; font-family: var(--font-sans); }\n.history-del:hover { background: #FBE9E7; border-color: #C0392B; color: #C0392B; }\n.hist-line { display: flex; align-items: center; flex-wrap: wrap; gap: 5px; font-size: 13px; color: var(--color-text-primary); }\n.hist-among { color: var(--color-text-secondary); }\n.hist-arrow { color: var(--color-text-tertiary); margin: 0 3px; }\n.expr-conn { font-size: 10px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.04em; color: var(--color-text-tertiary); margin: 0 2px; }\n.pct-big { font-size: 15px; font-weight: 600; color: var(--color-text-primary); font-family: var(--font-mono); }\n.pct-none { color: var(--color-text-warning); font-style: italic; }\n.pct-impossible { color: var(--color-text-warning); font-weight: 600; }\n.spec-empty { color: var(--color-text-tertiary); font-style: italic; }\n\n/* choice arena */\n.cc-progress { font-size: 12px; color: var(--color-text-tertiary); font-family: var(--font-mono); margin-bottom: 6px; }\n.cc-instructions { font-size: 15px; color: var(--color-text-primary); margin-bottom: 16px; }\n.cc-arena { display: flex; align-items: stretch; justify-content: center; gap: 12px; flex-wrap: wrap; animation: ccIn 0.28s ease; }\n@keyframes ccIn { from { opacity: 0; transform: translateY(8px); } to { opacity: 1; transform: none; } }\n/* all three choices are equal-sized cards */\n.cc-card { display: flex; flex-direction: column; flex: 1 1 0; min-width: 150px; max-width: 300px; background: var(--color-background-primary); border: 2px solid var(--color-border-tertiary); border-radius: var(--border-radius-lg); padding: 16px; cursor: pointer; text-align: left; font-family: var(--font-sans); transition: border-color 0.12s, box-shadow 0.12s, transform 0.06s, opacity 0.2s; }\n.cc-card:hover { border-color: var(--color-border-info); box-shadow: 0 4px 16px rgba(0,0,0,0.10); }\n.cc-card:active { transform: translateY(1px); }\n.cc-card-title { font-size: 12px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.05em; color: var(--color-text-secondary); margin-bottom: 12px; text-align: center; }\n.cc-attrs { display: flex; flex-direction: column; gap: 8px; align-items: center; }\n.cc-attr { display: flex; justify-content: center; }\n.cc-pick { margin-top: auto; padding-top: 14px; }\n.cc-pick span { display: block; font-size: 12px; font-weight: 600; color: var(--color-text-info); text-align: center; padding: 8px; border-radius: var(--border-radius-md); background: var(--color-background-info); transition: background 0.12s, color 0.12s; }\n/* the \"equal\" card */\n.cc-card-equal .cc-equal-body { flex: 1; display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 4px; color: var(--color-text-secondary); min-height: 60px; }\n.cc-equal-symbol { font-size: 34px; font-weight: 300; line-height: 1; color: var(--color-text-tertiary); }\n.cc-equal-text { font-size: 13px; }\n\n/* click feedback: selected card lights up, others fade, then it advances */\n.cc-arena.cc-locked .cc-card { cursor: default; }\n.cc-arena.cc-locked .cc-card:hover { box-shadow: none; }\n.cc-card.cc-dim { opacity: 0.3; border-color: var(--color-border-tertiary) !important; }\n.cc-card.cc-selected { border-color: var(--light2-fill); box-shadow: 0 0 0 4px var(--light2-bg); transform: translateY(-2px); }\n.cc-card.cc-selected .cc-pick span { background: var(--light2-fill); color: #fff; }\n.cc-card.cc-selected .cc-pick span::before { content: '\\2713\\00a0\\00a0'; }\n.cc-done { text-align: center; font-size: 16px; color: var(--light2-text); padding: 2rem; background: var(--light2-bg); border-radius: var(--border-radius-lg); }\n.cc-warn { font-size: 13px; color: var(--color-text-warning); font-style: italic; padding: 1rem; text-align: center; }\n\n/* examples wrapper: frozen, non-interactive replica of the live widgets */\n.pm-ex { font-family: var(--font-sans); max-width: 900px; margin: 12px 0 18px; color: var(--color-text-primary); pointer-events: none; user-select: none; }\n.pm-ex .database-box { height: 160px; }\n.pm-ex .qb-blank.empty { min-width: 160px; }\n.pm-ex .pm-choice { max-width: 820px; margin: 0; }\n/* the request itself, set off from the chip tray */\n.pm-ex .qb-request { border: 3px solid #3d3c38; border-radius: 10px; padding: 6px 14px 10px; margin-bottom: 14px; background: var(--color-background-primary); }\n.pm-ex .qb-request .qb-zone-line.big { padding-top: 8px; }\n";
(function () {
  var OUTCOME_SVG = '<svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>';

  function chip(term) {
    if (term.outcome) {
      var st = /^un/i.test(term.outcome) ? 'off' : 'on';
      return '<span class="mini-chip kind-sound state-' + st + '">' + OUTCOME_SVG + (st === 'on' ? 'successful' : 'unsuccessful') + '</span>';
    }
    var pos = term.attr | 0, on = /^h/i.test(String(term.value)) ? 'on' : 'off';
    return '<span class="mini-chip l' + pos + ' state-' + on + '"><span class="mini-bulb l' + pos + ' ' + on + '"></span>A' + pos + ' <span class="hilo">' + (on === 'on' ? 'high' : 'low') + '</span></span>';
  }
  function varChip(pos, label) {
    return '<span class="mini-chip kind-var l' + pos + '"><span class="mini-bulb l' + pos + ' on"></span>' + label + '</span>';
  }
  function pill(conn, ghost) {
    var isOr = conn === 'or';
    return '<button type="button" class="qb-conn' + (isOr ? ' or' : '') + (ghost ? ' ghost' : '') + '" tabindex="-1">' + (isOr ? 'or' : 'and') + '</button>';
  }
  // a filled blank: chips joined by and/or pills, grouped like the live builder
  function blank(terms, emptyClass) {
    if (!terms || !terms.length) return '<span class="qb-blank empty ' + (emptyClass || '') + '"></span>';
    var groups = [];
    terms.forEach(function (t, i) { if (i === 0 || t.conn === 'or') groups.push([t]); else groups[groups.length - 1].push(t); });
    var solo = groups.length === 1, html = '';
    groups.forEach(function (g, gi) {
      if (gi > 0) html += '<span class="qb-or-sep">' + pill('or') + '</span>';
      html += '<span class="qb-group' + (solo ? ' solo' : '') + '">';
      g.forEach(function (t, p) { if (p > 0) html += pill('and'); html += '<span class="qb-chip">' + chip(t) + '</span>'; });
      html += '</span>';
    });
    html += '<span class="qb-trailer">' + pill('and', true) + '<span class="qb-ghost-chip">+ chip</span></span>';
    return '<span class="qb-blank">' + html + '</span>';
  }
  function zone(text, terms, big, emptyClass) {
    return '<div class="qb-zone-line' + (big ? ' big' : '') + '"><span class="qb-text-lg">' + text + '</span>'
      + '<div class="qb-zone">' + blank(terms, emptyClass) + '<div class="qb-add-wrap"><button class="qb-add-btn mini" type="button" tabindex="-1">+</button></div></div></div>';
  }
  function tray(k) {
    var html = '';
    for (var p = 1; p <= k; p++) html += '<span class="qb-pal-group"><span class="qb-chip">' + chip({ attr: p, value: 'high' }) + '</span><span class="qb-chip">' + chip({ attr: p, value: 'low' }) + '</span></span>';
    html += '<span class="qb-pal-group"><span class="qb-chip">' + chip({ outcome: 'successful' }) + '</span><span class="qb-chip">' + chip({ outcome: 'unsuccessful' }) + '</span></span>';
    return '<div class="qb-palette-wrap"><div class="qb-palette-label">Chip tray <span>— attribute &amp; outcome chips; drag into a blank above</span></div><div class="qb-palette">' + html + '</div></div>';
  }
  function builder(k, conditions, target) {
    return '<div class="controls"><div class="qb-request">' + zone('Among candidates with', conditions, true, '') + zone('I want to know what percentage are', target, false, 'target-empty') + '</div>' + tray(k) + '</div>'
      + '<div class="actions-row"><button class="submit-btn" type="button" tabindex="-1"' + ((target && target.length) ? '' : ' disabled') + '>Show result</button><button class="ctrl-btn" type="button" tabindex="-1">Reset</button></div>';
  }
  function exprHTML(terms) { return terms.map(function (t, i) { return (i > 0 ? '<span class="expr-conn">' + (t.conn === 'or' ? 'or' : 'and') + '</span>' : '') + chip(t); }).join(''); }
  function resultLine(conditions, target, pct, num) {
    var cond = (conditions && conditions.length) ? exprHTML(conditions) : '<span class="spec-empty">all candidates</span>';
    return '<div class="history-section"><div class="history-header"><span>Saved results <span style="font-weight:400;color:var(--color-text-tertiary);">— drag to reorder</span></span><span style="font-size:11px;color:var(--color-text-tertiary);font-weight:400;">1 result</span></div>'
      + '<div class="history-item latest"><span class="drag-handle">⠿</span><span class="history-num">#' + (num || 1) + '</span><span class="hist-line"><span class="hist-among">Among candidates with</span> ' + cond
      + '<span class="hist-arrow">→</span><span class="pct-big">' + Number(pct).toFixed(1) + '%</span> <span class="hist-among">are</span> ' + exprHTML(target) + '</span></div></div>';
  }
  function emptyHistory() {
    return '<div class="history-section"><div class="history-header"><span>Saved results <span style="font-weight:400;color:var(--color-text-tertiary);">— drag to reorder</span></span></div><div class="history-empty">No results yet — build a question above and click "Show result" to add it here.</div></div>';
  }

  // demo data table: fixed pseudo-random rows, every attribute 50/50 and success 50%,
  // independent of everything — carries no information about the real data set
  function mulberry32(seed) { return function () { seed |= 0; seed = seed + 0x6D2B79F5 | 0; var t = seed; t = Math.imul(t ^ t >>> 15, t | 1); t ^= t + Math.imul(t ^ t >>> 7, t | 61); return ((t ^ t >>> 14) >>> 0) / 4294967296; }; }
  function demoTable(k, nRows) {
    var rng = mulberry32(7), head = '<th style="width: 80px;">Candidate</th>', rows = '';
    for (var p = 1; p <= k; p++) head += '<th>Attribute ' + p + '</th>';
    head += '<th>Outcome</th>';
    for (var r = 0; r < nRows; r++) {
      var id = 'C' + (1 + Math.floor(rng() * 9999)), cells = '';
      for (var q = 1; q <= k; q++) { var hi = rng() < 0.5; cells += '<td><span class="bulb-cell l' + q + ' ' + (hi ? 'on' : 'off') + '"></span> <span class="hilo">' + (hi ? 'high' : 'low') + '</span></td>'; }
      var s = (r % 2 === 0); // exactly half successful
      rows += '<tr><td class="machine-id">' + id + '</td>' + cells + '<td class="' + (s ? 'sound-yes' : 'sound-no') + '">' + (s ? '✓ successful' : 'unsuccessful') + '</td></tr>';
    }
    return '<h2>Candidate data</h2><div class="database-box"><table><thead><tr>' + head + '</tr></thead><tbody>' + rows + '</tbody></table></div>';
  }

  function choiceRound(spec) {
    var k = spec.nAttributes | 0;
    function card(vals, label) {
      var chips = '';
      for (var p = 1; p <= k; p++) chips += '<div class="cc-attr">' + chip({ attr: p, value: (vals && vals[p - 1]) || 'low' }) + '</div>';
      return '<button type="button" class="cc-card" tabindex="-1"><div class="cc-card-title">' + label + '</div><div class="cc-attrs">' + chips + '</div><div class="cc-pick"><span>Choose this candidate</span></div></button>';
    }
    var tie = spec.showTie === false ? '' : '<button type="button" class="cc-card cc-card-equal" tabindex="-1"><div class="cc-card-title">Tie</div><div class="cc-equal-body"><span class="cc-equal-symbol">=</span><span class="cc-equal-text">Equally likely</span></div><div class="cc-pick"><span>They’re equal</span></div></button>';
    return '<div class="pm-choice"><div class="cc-progress">Choice 1 of ' + (spec.numRounds || 42) + '</div><div class="cc-instructions">' + (spec.instructions || 'Which candidate do you think is more likely to be successful?') + '</div>'
      + '<div class="cc-arena">' + card(spec.a, 'Candidate A') + tie + card(spec.b, 'Candidate B') + '</div></div>';
  }

  // colours for attributes beyond the three hard-coded ones (same rule as the widgets)
  var styledExtra = {};
  function ensureLightStyles(k) {
    var css = '';
    for (var p = 5; p <= k; p++) {
      if (styledExtra[p]) continue; styledExtra[p] = true;
      var hue = Math.round((p * 47) % 360), fill = 'hsl(' + hue + ' 62% 52%)', stroke = 'hsl(' + hue + ' 62% 36%)', bg = 'hsl(' + hue + ' 62% 94%)', text = 'hsl(' + hue + ' 62% 28%)';
      var glow = 'hsl(' + hue + ' 62% 80%)', pale = 'hsl(' + hue + ' 62% 97%)', dot = 'hsl(' + hue + ' 62% 94%)', c = 'l' + p;
      css += '.bulb-cell.' + c + '.on{background:' + stroke + ';border-color:' + text + ';}';
      css += '.mini-bulb.' + c + '.on{background:' + stroke + ';border-color:' + text + ';}';
      css += '.mini-chip.' + c + '.state-on{background:#fff;border-color:' + stroke + ';}';
      css += '.mini-chip.kind-var.' + c + '{background:' + bg + ';border-color:' + fill + ';color:' + text + ';}';
      css += '.bulb-cell.' + c + '.off{background:' + dot + ';border-color:' + glow + ';}';
      css += '.mini-bulb.' + c + '.off{background:' + dot + ';border-color:' + glow + ';}';
      css += '.mini-chip.' + c + '.state-off{background:#fff;border-color:' + glow + ';}';
    }
    if (css) { var s = document.createElement('style'); s.textContent = css; document.head.appendChild(s); }
  }

  function render(el) {
    var spec; try { spec = JSON.parse(el.textContent.trim()); } catch (e) { el.innerHTML = '<div class="cc-warn">example spec is not valid JSON</div>'; return; }
    var k = Math.max(1, spec.nAttributes | 0);
    ensureLightStyles(k);
    var html = '';
    if (spec.kind === 'overview') {
      html = demoTable(k, spec.rows || 12) + builder(k, [], []) + emptyHistory();
    } else if (spec.kind === 'choice') {
      html = choiceRound(spec);
    } else { // query
      var showBuilder = spec.showBuilder !== false, showResult = !!spec.showResult;
      if (showBuilder) html += builder(k, spec.conditions || [], spec.target || []);
      if (showResult) html += resultLine(spec.conditions || [], spec.target || [], spec.pct == null ? 50 : spec.pct, spec.num);
    }
    el.innerHTML = '<div class="pm-ex">' + html + '</div>';
    el.style.display = '';
    el.setAttribute('data-pm-rendered', '1');
  }

  function boot() {
    var els = document.querySelectorAll('.pm-example:not([data-pm-rendered])');
    if (!els.length) return setTimeout(boot, 40);
    if (!document.getElementById('pm-examples-css')) { var st = document.createElement('style'); st.id = 'pm-examples-css'; st.textContent = CSS; (document.head || document.documentElement).appendChild(st); }
    for (var i = 0; i < els.length; i++) render(els[i]);
  }
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', boot); else boot();
})();

})();
